class Solution {
    fun solution(phone_number: String): String {
        var point = phone_number.length-4
        var answer = "*".repeat(point) + phone_number.substring(point, )
        return answer
    }
}